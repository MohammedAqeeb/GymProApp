name: flutter_bloc_app_complete
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  hydrated_bloc: ^9.1.2
  intl: ^0.17.0
  numberpicker: ^2.1.2
  path_provider: ^2.1.1
  platform: ^3.1.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/workouts.json
    - assets/stopwatch.png
    - assets/beep.oga
    - assets/ding.oga
    - assets/applause.oga
